#include<stdio.h>
#include<math.h>
int main()
{
 float area,b,h;
 printf("Enter the base and height \n:");
 scanf("%f%f",&b,&h);
 area=(0.5*b*h);
 printf("Area of the triangle is %f",area);
 return 0;
}
