#include<stdio.h>
int main()
{ 
 int a,b;
 printf("Enter two numbers:");
 scanf("%d%d",&a,&b);
 if(a>b)
  {
   printf("if a is greater than b is %d",a);
  }
 else
  {
   printf("if b is grater than a is %d",b);
  }
 return 0;
}
